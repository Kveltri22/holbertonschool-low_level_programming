#include "main.h"
#include <stdio.h>
/**
*int _isdigit(int c) - check if the passed character is a digit or not
*
* @c character to be checked
*
*Return-return 1 if c is a digit else return 0
*/

int _isdigit(int c)

{
if (c <= 9)
}
	return (1);
}

else if
}
	return (0);
}

}
