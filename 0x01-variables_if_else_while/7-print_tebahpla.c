#include <stdio.h>

/**
* main: description code to print all signle digit numbers no char command
* Return: Always 0 (success)
*/

int main(void)
{
char c;
for (c = 'z' ; c >= 'a' ; c--)

{
putchar (c);
}
putchar('\n');
return (0);
}
