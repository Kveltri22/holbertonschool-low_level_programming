#include <stdio.h>
/**
* Main - Print the alaphabet in lowercase
* Return: Alway 0 (Success)
*/

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z' ; c++)

	{
		putchar(c);
	}
	putchar ('\n');
	return (0);
}
